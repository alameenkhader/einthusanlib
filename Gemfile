source 'https://rubygems.org'

# Web framework: Sinatra (Rack 3).
gem 'puma', '>= 5.0'
gem 'rackup', '~> 2.2'
gem 'sinatra', '~> 4.1'

gem 'rake'

group :development do
  gem 'rubocop', require: false
end

group :test do
  gem 'minitest', require: false
  gem 'rack-test', require: false
end
