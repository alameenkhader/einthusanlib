[tools]
ruby = "3.2.3"
python = "3.11"

[env]
_.python.venv = { path = "venv", create = true, uv_create_args = ["--seed"] }

[tasks.setup]
description = "One-stop setup: venv, youtube-dl, gems, db"
run = [
  "venv/bin/pip install youtube-dl",
  "bundle install",
  "bin/rails db:prepare",
]